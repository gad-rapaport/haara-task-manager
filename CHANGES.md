
# 📝 CHANGES.md - יומן שינויים

## [סבב ב' - 17/05/2026] מקצה שיפורים, סגירת רגרסיות ואמינות תיעוד

סבב זה בוצע כמענה ישיר ל-Code Review, עם דגש על סגירת קצוות טכניים, שיפור UX, סנכרון מלא בין הקוד לתיעוד וטיפול במקרי קצה.

### 🧠 Backend (Python + FastAPI)

- החלת ולידציית `TaskCreate` גם על מסלול ה‑AI.
- משימות עם ערכים לא חוקיים נחסמות ולא נשמרות ל‑DB.
- שיפור Prompt Engineering למניעת יצירת משימות שווא.
- נוסף שדה `tasksCreated` לטובת חיווי מדויק בפרונט.
- נוספה תמיכה בתאריך דינמי למשימות.
- נוסף טיפול במקרה קצה של קובץ DB ריק.
- תיקון בעיות `UnicodeEncodeError` / `charmap` ב‑Windows.
- נאכף `encoding="utf-8"` בכל קריאה/כתיבה.

### 🎨 Frontend (React + Vite)

- החזרת שדות סינון לפי `owner` ו‑`currentHandler`.
- הוחזר Date Picker להזנת סיכומים רטרואקטיבית.
- נוסף סטטוס `done` לסינון.
- נוספה ולידציה המונעת שמירת משימה ללא כותרת.
- שופר מנגנון Toast:
  - `tasksCreated > 0` → הצלחה
  - `tasksCreated === 0` → הודעת מידע

### 🔐 אבטחה ויציבות

- הגנה מפני CSV Injection.
- ריכוז כל קריאות ה‑API ב‑`apiService.js`.
- העברת `axios` ו‑`lucide-react` ל‑dependencies.

### 📚 תיעוד

- סנכרון מלא מול README.
- הסרת פיצ'רים שלא קיימים בפועל.
